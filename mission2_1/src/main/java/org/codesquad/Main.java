package org.codesquad;

import org.codesquad.phone.Phone;
import org.codesquad.phone.PhoneBook;
import org.codesquad.rectangle.Rectangle;

import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        /*
        // 문제 #1
        Scanner scanner = new Scanner(System.in);
        System.out.print("수학, 과학, 영어 순으로 3개의 정수 입력 >> ");
        int mathScore = scanner.nextInt();
        int scienceScore = scanner.nextInt();
        int englishScore = scanner.nextInt();

        Grade me = new Grade(mathScore, scienceScore, englishScore);
        System.out.println("평균은 " + me.scoreAverage()); // 평균을 계산하여 리턴
        scanner.close();
        */

        /*
        // 문제 #2
        Rectangle r = new Rectangle(2, 2, 8, 7);
        Rectangle s = new Rectangle(5, 5, 6, 6);
        Rectangle t = new Rectangle(1, 1, 10, 10);
        r.show();
        System.out.println("s의 면적은 "+s.square());
        if(t.contains(r)) System.out.println("t는 r을 포함합니다.");
        if(t.contains(s)) System.out.println("t는 s를 포함합니다.");
        */


    }
}
